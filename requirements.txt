# Serving dependencies only. Training additionally needs requirements-train.txt.
# Direct dependencies are pinned so a Hugging Face Space rebuild cannot pick up a
# breaking upstream release. Transitive dependencies are left unpinned because
# these pins were validated on CPython 3.12 only.
# The extra index serves CPU-only torch builds. The PyPI Linux wheel pulls in
# about 4 GB of CUDA libraries that a CPU Space can never use. If the extra index
# is unreachable pip falls back to PyPI and the build still succeeds.
--extra-index-url https://download.pytorch.org/whl/cpu

flask==3.1.3
transformers==5.16.1
torch==2.14.0
gunicorn==26.2.0
